version: '3.8'

services:
  calendar-organizer:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    container_name: calendar-organizer-app
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
